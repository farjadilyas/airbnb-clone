apiVersion: v1
kind: Service
metadata:
    name: airbnb-payment
spec:
    selector:
        app: airbnb-payment
    ports:
        - protocol: TCP
          port: 80
          targetPort: 3000
    type: ClusterIP
